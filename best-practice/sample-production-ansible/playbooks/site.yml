---
- name: Apply baseline configuration to all hosts
  hosts: all
  become: true
  roles:
    - common

- name: Configure web tier
  hosts: web
  become: true
  roles:
    - nginx

- name: Deploy application tier
  hosts: app
  become: true
  roles:
    - application
