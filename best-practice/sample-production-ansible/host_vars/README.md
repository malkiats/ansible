# Host Variables

Use this directory for host-specific overrides only when a value truly applies to one machine.

Prefer `group_vars/` whenever a setting is shared by more than one host. Overusing `host_vars/` usually creates duplication and makes inventories harder to maintain.
